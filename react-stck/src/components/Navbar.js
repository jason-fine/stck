import React from 'react'
import styled from 'styled-components'

const NavbarContainer = styled.div`
  background-color: green;
`

const Navbar = () => {
  return (
    <NavbarContainer> Register Login</NavbarContainer>
  )
}

export default Navbar